import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts', (WidgetTester tester) async {
    // Placeholder test - Hive needs initialization for full testing
    expect(true, isTrue);
  });
}
